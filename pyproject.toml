[project]
name = "ane-surrogate"
version = "0.1.0"
description = "Surrogate model for building energy prediction via MCP"
requires-python = ">=3.10,<3.13"
dependencies = [
    "mcp[cli]",
    "numpy",
]
